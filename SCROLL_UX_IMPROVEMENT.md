# 📜 채팅 스크롤 UX 개선

## 🎯 문제점
> "해설을 하거나 채팅을 하면 내용이 출력되면서 하단으로 내려가서 읽기가 조금 불편한데 좋은 방법은?"

---

## ✅ 적용된 개선사항

### 1. **스마트 자동 스크롤**
```javascript
// Before: 무조건 맨 아래로 스크롤
box.scrollTop = box.scrollHeight;

// After: 사용자 위치 고려
const wasAtBottom = box.scrollHeight - box.scrollTop <= box.clientHeight + 100;

if (sender === 'user' || wasAtBottom) {
    // 사용자가 맨 아래 근처에 있을 때만 스크롤
    box.scrollTop = box.scrollHeight;
}
```

**효과:**
- ✅ 사용자가 위쪽 내용을 읽고 있으면 스크롤 안 함
- ✅ 맨 아래에 있으면 새 메시지 따라감
- ✅ 자연스러운 읽기 경험

---

### 2. **플로팅 스크롤 버튼**

#### 맨 위로 버튼 (⬆️)
```
위치: 우측 하단
표시: AI 응답 완료 후 자동 표시
기능: 부드럽게 맨 위로 스크롤
색상: 보라 그라데이션
```

#### 맨 아래로 버튼 (⬇️)
```
위치: 우측 하단 (맨 위로 버튼 아래)
표시: 항상 표시
기능: 부드럽게 맨 아래로 스크롤
색상: 블루 그라데이션
```

**UI 배치:**
```
채팅 박스
    ├── 메시지들...
    └── [우측 하단]
        ├── ⬆️ 맨 위로 (조건부)
        └── ⬇️ 맨 아래로 (항상)
```

---

### 3. **부드러운 스크롤 애니메이션**
```javascript
box.scrollTo({
    top: 0,  // or box.scrollHeight
    behavior: 'smooth'  // 부드러운 애니메이션
});
```

**효과:**
- ✅ 갑작스럽게 튀지 않음
- ✅ 어디로 이동하는지 눈으로 추적 가능
- ✅ 현대적인 UX

---

## 🎨 사용자 경험 시나리오

### 시나리오 1: 긴 사주 해설 읽기
```
1. 사주 분석 요청
   → AI가 긴 해설 작성 중
   
2. 사용자가 해설 처음부터 읽기 시작
   → 위쪽으로 스크롤
   
3. AI가 추가 내용 출력
   ❌ Before: 갑자기 맨 아래로 튐 (읽던 곳을 잃어버림)
   ✅ After: 그대로 유지 (계속 읽을 수 있음)
   
4. 다 읽고 ⬇️ 버튼 클릭
   → 부드럽게 맨 아래로 이동
```

### 시나리오 2: 여러 운세 비교
```
1. 일간 운세 확인
   → 내용 읽기
   
2. 주간 운세 클릭
   → 새 내용 추가됨
   → ⬆️ 버튼 자동 표시
   
3. ⬆️ 클릭
   → 부드럽게 맨 위로 스크롤
   → 일간 운세부터 다시 비교 가능
```

### 시나리오 3: 채팅 대화
```
1. 사용자: 질문 입력 후 전송
   → 자동으로 맨 아래 (자기 메시지 확인)
   
2. AI: 답변 생성 중
   → 사용자가 이전 내용 다시 보려고 위로 스크롤
   
3. AI: 답변 완료
   ❌ Before: 갑자기 맨 아래로 (읽던 곳 놓침)
   ✅ After: 그대로 유지 (계속 읽을 수 있음)
   
4. ⬇️ 버튼으로 새 답변 확인
```

---

## 🔧 구현 세부사항

### 1. appendMsg() 함수 개선
```javascript
function appendMsg(sender, text) {
    const box = document.getElementById('chat-box');
    const div = document.createElement('div');
    
    // 현재 위치 체크 (맨 아래에서 100px 이내면 아래 있다고 판단)
    const wasAtBottom = box.scrollHeight - box.scrollTop <= box.clientHeight + 100;
    
    // 메시지 추가
    if (sender === 'ai') {
        div.className = 'ai-msg';
        div.innerHTML = text.replace(/\n/g, '<br>')
                            .replace(/\*\*(.*?)\*\*/g, '<span class="strong-hl">$1</span>');
    } else {
        div.className = 'user-msg';
        div.textContent = text;
    }
    box.appendChild(div);
    
    // 스마트 스크롤
    if (sender === 'user' || wasAtBottom) {
        box.scrollTop = box.scrollHeight;
    }
    
    // AI 응답 완료 시 "맨 위로" 버튼 표시
    if (sender === 'ai' && !text.includes('분석 중') && !text.includes('계산 중')) {
        setTimeout(() => showScrollToTopButton(), 500);
    }
}
```

### 2. 스크롤 버튼 함수
```javascript
function showScrollToTopButton() {
    const btn = document.getElementById('scroll-to-top-btn');
    if (btn) {
        btn.style.display = 'block';
        setTimeout(() => btn.style.opacity = '1', 10);
    }
}

function hideScrollToTopButton() {
    const btn = document.getElementById('scroll-to-top-btn');
    if (btn) {
        btn.style.opacity = '0';
        setTimeout(() => btn.style.display = 'none', 300);
    }
}

function scrollToTop() {
    const box = document.getElementById('chat-box');
    box.scrollTo({ top: 0, behavior: 'smooth' });
    hideScrollToTopButton();
}

function scrollToBottom() {
    const box = document.getElementById('chat-box');
    box.scrollTo({ top: box.scrollHeight, behavior: 'smooth' });
}
```

### 3. 스크롤 이벤트 감지
```javascript
chatBox.addEventListener('scroll', function() {
    const isAtTop = chatBox.scrollTop < 100;
    const scrollTopBtn = document.getElementById('scroll-to-top-btn');
    
    // 맨 위에 있으면 "맨 위로" 버튼 숨김
    if (isAtTop && scrollTopBtn.style.display === 'block') {
        hideScrollToTopButton();
    }
});
```

---

## 🎨 UI 디자인

### 스크롤 버튼 스타일
```css
/* 맨 위로 버튼 */
width: 45px
height: 45px
border-radius: 50%
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%)
position: absolute
right: 15px
bottom: 70px
transition: all 0.3s

/* 맨 아래로 버튼 */
width: 45px
height: 45px
border-radius: 50%
background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%)
hover: scale(1.1)
```

### 버튼 배치
```
채팅 입력창 (bottom: 0)
       ↑ 70px
⬇️ 맨 아래로 버튼
       ↑ 8px (gap)
⬆️ 맨 위로 버튼 (조건부)
```

---

## 📊 동작 로직 흐름도

```
메시지 추가 요청
    ↓
현재 스크롤 위치 체크
    ↓
┌─────────────┬─────────────┐
│ 맨 아래 근처 │  위쪽에 있음  │
└─────────────┴─────────────┘
       ↓              ↓
   자동 스크롤    스크롤 안 함
       ↓              ↓
   새 메시지 표시   사용자 계속 읽기
       ↓              ↓
AI 응답 완료?      ⬆️ 버튼 표시
       ↓              ↓
  ⬆️ 버튼 표시    사용자가 원할 때
       ↓          ⬆️⬇️ 클릭
   사용자 선택
```

---

## ✅ 개선 효과

### Before
```
❌ 메시지가 올 때마다 무조건 맨 아래로
❌ 긴 해설 읽는 중에 끊김
❌ 이전 내용 다시 보기 어려움
❌ 갑작스러운 스크롤로 혼란
```

### After
```
✅ 읽고 있으면 스크롤 안 함
✅ 자연스러운 읽기 흐름
✅ 버튼으로 쉽게 이동
✅ 부드러운 애니메이션
✅ 사용자가 제어권 가짐
```

---

## 🎯 사용자 피드백 예상

### 긴 해설 읽을 때
```
"읽고 있는데 갑자기 아래로 안 내려가니까 편하네요!"
```

### 여러 운세 비교할 때
```
"위로 버튼 눌러서 처음부터 다시 보기 편해요!"
```

### 채팅할 때
```
"이전 대화 보다가 새 답변 오면 아래 버튼으로 바로 가니까 좋아요!"
```

---

## 🧪 테스트 체크리스트

- [ ] 사주 분석 시 긴 해설 출력
  - [ ] 위로 스크롤하면 고정됨
  - [ ] 맨 아래 있으면 자동 스크롤

- [ ] ⬆️ 버튼 동작
  - [ ] AI 응답 완료 후 표시
  - [ ] 클릭 시 부드럽게 맨 위로
  - [ ] 맨 위 도착하면 자동 숨김

- [ ] ⬇️ 버튼 동작
  - [ ] 항상 표시
  - [ ] 클릭 시 부드럽게 맨 아래로
  - [ ] 호버 시 크기 증가

- [ ] 채팅 중 동작
  - [ ] 사용자 메시지는 항상 따라감
  - [ ] AI 응답은 위치 고려
  - [ ] 버튼으로 자유롭게 이동

---

## 💡 추가 고려사항

### 1. 모바일 대응
```
터치 스크롤에도 정상 작동
버튼 크기 충분 (45px)
터치 영역 명확
```

### 2. 접근성
```
title 속성으로 버튼 기능 설명
키보드로도 접근 가능
명확한 시각적 피드백
```

### 3. 성능
```
debounce 불필요 (간단한 체크만)
메모리 누수 없음
부드러운 애니메이션
```

---

## 🎉 결과

**사용자가 편하게 긴 내용을 읽을 수 있는 채팅 환경 완성!**

- ✅ 읽기 중 방해 없음
- ✅ 원하는 위치로 쉽게 이동
- ✅ 자연스러운 UX
- ✅ 현대적인 인터페이스
